# expense-tracker-app
Built a personal finance dashboard — Ledger — with React, TypeScript, and Tailwind CSS.  Features: → Transaction CRUD with real-time form validation → Category and date-range filtering → Monthly spending trends (Recharts) → CSV export → Dark mode → Fully persisted with localStorage
